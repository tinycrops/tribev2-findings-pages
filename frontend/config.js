// These findings pages are fully static (self-contained HTML with embedded figures).
// No backend is required. CONFIG is kept for forward-compatibility; frontend code
// must tolerate it being undefined or BACKEND_URL being empty.
const CONFIG = {
  BACKEND_URL: "",
};
